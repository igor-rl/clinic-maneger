import { Model } from "sequelize";

export interface IPaginatedModel extends Model {
  // Aqui, você pode definir quaisquer métodos ou propriedades que todos os modelos que usam este caso de uso devem ter.
}
